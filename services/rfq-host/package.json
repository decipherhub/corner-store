{
  "name": "@corner-store/rfq-host",
  "version": "0.1.0",
  "private": true,
  "description": "Production-separable hardened HTTP host boundary for Corner Store RFQ coordinators.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/test/smoke.js"
  },
  "dependencies": {
    "@corner-store/rfq-service": "file:../rfq"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
