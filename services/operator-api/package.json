{
  "name": "@corner-store/operator-api",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only operator snapshot and event API for Corner Store tooling.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/test/smoke.js",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@corner-store/toolkit": "file:../toolkit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
