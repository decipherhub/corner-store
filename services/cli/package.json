{
  "name": "@corner-store/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive reference CLI for driving the Corner Store stack against a live node (CLI-001).",
  "bin": {
    "corner-store": "dist/cli/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli/src/index.js",
    "test": "npm run build && node dist/cli/test/smoke.js"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
