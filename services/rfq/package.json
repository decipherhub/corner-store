{
  "name": "@corner-store/rfq-service",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript SDK helpers for Corner Store RFQ quote backends.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/test/smoke.js"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
