{
  "name": "@corner-store/deployment-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Local deployment control surface for Corner Store reference execution and production planning.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/test/smoke.js",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@corner-store/toolkit": "file:../toolkit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.3"
  }
}
